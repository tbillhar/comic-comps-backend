apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: comic-comps-backend
spec:
  template:
    spec:
      containers:
        - image: gcr.io/PROJECT_ID/comic-comps-backend:latest
          ports:
            - containerPort: 8080
          env:
            - name: ENVIRONMENT
              value: production
            - name: CORS_ORIGINS
              value: https://jolly-longma-5797b4.netlify.app
            - name: COMPS_PROVIDER
              value: sample
